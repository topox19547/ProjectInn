enum MessageType{
    CREATE_GAME,
    JOIN_GAME,
    TOKEN_MOVING,
    TOKEN_MOVED,
    TOKEN,
    TOKEN_OWNERSHIP,
    TOKEN_ATTRIBUTE,
    TOKEN_NOTE,
    ASSET,
    PERMISSIONS,
    MAP_CHANGED,
    MAP_PING,
    CHAT,
    PING,
    ERROR
}